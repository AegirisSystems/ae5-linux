# Optional template. Not installed or enabled by the driver package.
context.objects = [
    { factory = adapter
      args = {
          factory.name = support.null-audio-sink
          node.name = "aegaudio_virtual_main"
          node.description = "AegAudio Virtual Main"
          media.class = Audio/Sink
          audio.position = [ FL FR ]
          monitor.channel-volumes = true
          monitor.passthrough = true
      }
    }
]
